{
  "config": {
    "step": {
      "user": {
        "title": "Add SBM station",
        "description": "Choose an S-Bahn München station.",
        "data": {
          "station_id": "Station",
          "name": "Name"
        }
      }
    },
    "error": {
      "invalid_station": "Station could not be found."
    },
    "abort": {
      "already_configured": "This station is already configured.",
      "invalid_station": "Station could not be found."
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "destinations": "Destinations (comma-separated)",
          "lines": "Lines (comma-separated)",
          "timeoffset": "Time offset (minutes)",
          "number": "Number of departures"
        }
      }
    }
  },
  "issues": {
    "deprecated_yaml": {
      "title": "The SBM YAML configuration is deprecated",
      "description": "SBM stations configured in `configuration.yaml` have been imported automatically as config entries. You can now remove the `sensor: - platform: sbm` block from `configuration.yaml`."
    }
  }
}
