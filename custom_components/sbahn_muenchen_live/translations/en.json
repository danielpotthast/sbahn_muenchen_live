{
  "config": {
    "step": {
      "user": {
        "title": "Add SBM station",
        "description": "Choose an S-Bahn München station.",
        "data": {
          "station_id": "Station",
          "name": "Name"
        }
      }
    },
    "error": {
      "invalid_station": "Station could not be found."
    },
    "abort": {
      "already_configured": "This station is already configured.",
      "invalid_station": "Station could not be found."
    }
  },
  "options": {
    "step": {
      "init": {
        "description": "Destinations must match the exact text shown in the sensor's `departures` attribute (e.g. \"Flughafen/Airport ✈\", not a shortened station name) — leave empty first, check the attribute, then fill this in.",
        "data": {
          "destinations": "Destinations (comma-separated)",
          "lines": "Lines (comma-separated)",
          "timeoffset": "Time offset (minutes)",
          "number": "Number of departures"
        },
        "data_description": {
          "destinations": "Exact destination text as shown live, e.g. \"Flughafen/Airport ✈\"",
          "lines": "Exact line names as shown live, e.g. \"S1, S8\""
        }
      }
    }
  },
  "issues": {
    "deprecated_yaml": {
      "title": "The S-Bahn München YAML configuration is deprecated",
      "description": "S-Bahn München stations configured in `configuration.yaml` have been imported automatically as config entries. You can now remove the `sensor: - platform: sbahn_muenchen_live` block from `configuration.yaml`."
    }
  }
}
